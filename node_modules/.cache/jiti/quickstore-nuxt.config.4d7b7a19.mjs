"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0; // nuxt.config.ts
var _default = exports.default = defineNuxtConfig({
  // 1. Core Modules for Startup Speed & Scalability
  modules: [
  '@nuxtjs/tailwindcss',
  '@nuxtjs/supabase'],


  // 2. Supabase Configuration (Security & Real-time)
  supabase: {
    redirect: false, // Set to true once Auth pages are built to protect the dashboard
    cookieOptions: {
      maxAge: 60 * 60 * 8, // 8 hours session for vendor shifts
      sameSite: 'lax',
      secure: true
    }
  },

  // 3. Tailwind Configuration (Optimized for Mobile/PWA)
  tailwindcss: {
    exposeConfig: true,
    viewer: false // Disable in production to reduce bundle size
  },

  // 4. App Headers & SEO (Critical for local search in Uganda)
  app: {
    head: {
      title: 'QuickStore Vendor Portal',
      meta: [
      { name: 'viewport', content: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0' },
      { charset: 'utf-8' },
      { name: 'description', content: 'Hyper-local grocery fulfillment for QuickStore partners.' }]

    }
  },

  // 5. Environment Variables Guardrail
  runtimeConfig: {
    public: {
      // These will be pulled from your .env file
      supabaseUrl: process.env.SUPABASE_URL,
      supabaseKey: process.env.SUPABASE_KEY
    }
  },

  devtools: { enabled: true }
}); /* v9-2ac2f82fdafabdb0 */
